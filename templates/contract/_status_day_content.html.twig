{#
    Day cell content for the working time overview.
    @see contract/status.html.twig

    Plugins can override this template to customize the rendering.
    Available variables: day, user, decimal

    Example override in a plugin:
    YourBundle/Resources/views/contract/_status_day_content.html.twig
#}
{% from "macros/widgets.html.twig" import work_times_result %}
{% if day.workingTime.expectedTime == 0 and day.workingTime.actualTime == 0 %}
    --:--
{% else %}
    {{ work_times_result(day.workingTime.expectedTime, day.workingTime.actualTime, decimal) }}
{% endif %}
