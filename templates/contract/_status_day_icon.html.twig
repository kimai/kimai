{#
    Icon for a day cell in the working time overview.
    @see contract/_status_day.html.twig

    Plugins can override this template to customize the icon rendering.
    Available variables: style, hasTooltip
#}
{%- if style is not null and hasTooltip -%}
<i class="{{ style|icon }} fa-xs"></i>
{%- endif -%}
