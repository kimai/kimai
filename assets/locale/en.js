
// en is the default locale in all apps, so this file is not really required
// but it is added, so we do not need to add locale specific checks in the frontend:
// we can always include the locale file