version: '3.8'

services:

  sqldb:
    image: mysql:8.3.0
    restart: unless-stopped
    volumes:
      - mysql:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=${MYSQL_DATABASE}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    command: --default-storage-engine innodb
    healthcheck:
      test: mysqladmin -p$$MYSQL_ROOT_PASSWORD ping -h localhost
      interval: 20s
      start_period: 10s
      timeout: 10s
      retries: 3
    networks:
      - kimai_net

  nginx:
    image: tobybatch/nginx-fpm-reverse-proxy:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"  # Optional: If you're terminating SSL
    volumes:
      - public:/opt/kimai/public:ro
    depends_on:
      - kimai
    healthcheck:
      test: wget --spider http://nginx/health || exit 1
      interval: 20s
      start_period: 10s
      timeout: 10s
      retries: 3
    networks:
      - kimai_net

  kimai:
    image: kimai/kimai2:latest
    restart: unless-stopped
    environment:
      - ADMINMAIL=${ADMINMAIL}
      - ADMINPASS=${ADMINPASS}
      - DATABASE_URL=mysql://${DB_USER}:${DB_PASS}@${DB_HOST}/${DB_NAME}?charset=utf8mb4&serverVersion=${DB_VERSION}
      - APP_ENV=prod
      - APP_DEBUG=0
      - PHP_INI_OPCACHE__VALIDATE_TIMESTAMPS=1
      - PHP_INI_OPCACHE__REVALIDATE_FREQ=0
      # SAML Configuration
      - SAML_ACTIVATE=true
      - SAML_TITLE=Azure AD Login
      - SAML_PROVIDER=azure
      - SAML_CONNECTION_BASE_URL=https://timesheet.shorthills.ai
      - SAML_IDP_ENTITY_ID=https://sts.windows.net/85a77b6c-a790-4bcf-8fd6-c1f891dd360b/
      - SAML_IDP_SSO_URL=https://login.microsoftonline.com/85a77b6c-a790-4bcf-8fd6-c1f891dd360b/saml2
      - SAML_IDP_SLO_URL=https://login.microsoftonline.com/85a77b6c-a790-4bcf-8fd6-c1f891dd360b/saml2
      - SAML_CONNECTION_STRICT=true
    volumes:
      - public:/opt/kimai/public
      - plugins:/opt/kimai/var/plugins
      - ./src:/opt/kimai/src
      - ./TimeTrack.cer:/opt/kimai/TimeTrack.cer:ro
    networks:
      - kimai_net

volumes:
  data:
  public:
  mysql:
  plugins:

networks:
  kimai_net:
    driver: bridge